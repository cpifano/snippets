#---------------------------------------------------------------------------------------------------------------------------#
# MYSQL:
#---------------------------------------------------------------------------------------------------------------------------#
#Editar settings.py en la seccion DATABASES:
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'OPTIONS': {
            'read_default_file': '/path/to/my.cnf',
        },
    }
}
#---------------------------------------------------------------------------------------------------------------------------#
#Contenido archivo my.cnf:
[client]
database = NAME
user = USER
password = PASSWORD
default-character-set = utf8
#---------------------------------------------------------------------------------------------------------------------------#
